# API 接口自动化测试 Agent - 使用说明

## ✅ 项目已完成

所有核心代码和文档已创建完成！

## 📦 文件清单

### 核心代码文件
- **api_test_agent.py** - 主程序（CLI + Python API）
- **config.py** - 配置管理模块
- **api_client.py** - HTTP 客户端封装
- **assert_engine.py** - 断言引擎
- **test_runner.py** - 测试执行器
- **report_generator.py** - 报告生成器
- **utils.py** - 工具函数

### 配置文件
- **requirements.txt** - Python 依赖（requests, PyYAML）

### 文档
- **README.md** - 快速开始指南
- **SKILL.md** - 完整技能文档和使用说明

### 示例
- **examples/sample_tests.yaml** - 完整的示例测试用例（6 个测试场景）

### 输出
- **reports/** - 测试报告输出目录（已包含 2 次运行测试的 HTML 和 JSON 报告）

## 🚀 如何开始使用

### 1. 安装依赖
```bash
cd D:\qwenpaw\workspaces\default\output\api-test-agent
pip install -r requirements.txt
```

### 2. 运行示例测试
```bash
python api_test_agent.py run examples/sample_tests.yaml --reports html,json
```

### 3. 查看报告
生成的报告会保存在 `reports/` 目录下：
- HTML 报告：双击打开即可在浏览器中查看
- JSON 报告：可用于集成到其他系统

## 🎯 功能特性

### 支持的 HTTP 方法
- GET
- POST
- PUT
- DELETE
- PATCH

### 断言类型
| 类型 | 说明 |
|------|------|
| status_code | HTTP 状态码检查 |
| equal | 值相等检查 |
| not_equal | 值不等检查 |
| contains | 容器包含检查 |
| not_contains | 容器不包含检查 |
| contains_key | 字典键存在检查 |
| type_check | 数据类型检查 |
| length | 长度检查 |
| regex_match | 正则表达式匹配 |
| response_time | 响应时间检查 |

### 高级功能
- ✅ 变量提取和传递（步骤间数据共享）
- ✅ 环境变量引用（${variable_name}）
- ✅ 多格式报告（HTML/Markdown/JSON）
- ✅ 重试机制（网络错误自动重试）
- ✅ 日志记录（控制台 + 文件）

## 💡 快速上手示例

### 创建你的第一个测试

创建文件 `tests/my_test.yaml`:

```yaml
name: "我的第一个 API 测试"
description: "测试一个简单的 GET 请求"

steps:
  - name: "获取用户信息"
    method: GET
    endpoint: "https://jsonplaceholder.typicode.com/users/1"
    
    assertions:
      - type: status_code
        expected: 200
      
      - type: contains_key
        key: "name"
        path: "$"
      
      - type: response_time
        max: 3.0
```

运行测试：
```bash
python api_test_agent.py run tests/my_test.yaml
```

### 带认证的测试

```yaml
name: "JWT 认证流程"
steps:
  # 登录获取 token
  - name: "登录"
    method: POST
    endpoint: "/oauth/token"
    json:
      client_id: "${CLIENT_ID}"
      client_secret: "${CLIENT_SECRET}"
    
    extract:
      access_token: "access_token"
    
    assertions:
      - type: status_code
        expected: 200
  
  # 使用 token 调用 API
  - name: "获取用户信息"
    method: GET
    endpoint: "/api/v1/user/profile"
    headers:
      Authorization: "Bearer ${access_token}"
    
    assertions:
      - type: status_code
        expected: 200
```

## 📖 CLI 命令参考

```bash
# 显示帮助
python api_test_agent.py --help

# 运行单个测试文件
python api_test_agent.py run test.yaml

# 运行目录下所有测试
python api_test_agent.py run tests/

# 指定报告格式
python api_test_agent.py run test.yaml --reports html,json,markdown

# 指定 API 基础 URL
python api_test_agent.py run test.yaml --base-url https://api.example.com

# 初始化新项目
python api_test_agent.py init -d my-api-project
```

## 🔧 Python API 使用

```python
from test_runner import TestRunner
from report_generator import ReportGenerator

# 初始化 runner
runner = TestRunner(base_url="https://api.example.com")

# 执行测试
result = runner.execute_test_file("tests/sample_test.yaml")

# 生成报告
generator = ReportGenerator(output_dir="reports")
html_report = generator.generate_html_report(result)

print(f"报告已生成：{html_report}")

# 关闭连接
runner.close()
```

## 📊 测试结果示例

```
============================================================
测试结果摘要
============================================================

  项目                     值
  -----------------------------------
  测试套件            sample_tests
  总用例数                 6
  通过                      2
  失败                      4
  通过率                  33.3%
  总耗时                  3.781s

  [INFO] 有 4 个测试失败
============================================================

[OK] HTML: reports/api_test_report_20260501_132319.html
[OK] JSON: reports/api_test_report_20260501_132319.json
```

## ⚙️ 配置说明

可以在 `config.json` 中设置全局配置：

```json
{
  "base_url": "https://api.example.com",
  "timeout": 30,
  "retry_times": 3,
  "retry_delay": 1,
  "headers": {
    "Content-Type": "application/json"
  },
  "log_level": "INFO",
  "log_file": "api_test.log"
}
```

## 🐛 注意事项

1. **Windows 编码问题**：已在代码中处理，不会影响正常使用
2. **网络连接**：确保可以访问目标 API 服务器
3. **超时设置**：默认 30 秒，可根据实际情况调整
4. **变量路径**：使用 `$` 表示根节点，`data.id` 表示嵌套字段

## 📝 下一步

1. 阅读 `SKILL.md` 了解更多高级用法
2. 修改 `examples/sample_tests.yaml` 作为参考
3. 创建你自己的测试用例
4. 集成到 CI/CD 流水线

## 🎉 祝你测试愉快！
